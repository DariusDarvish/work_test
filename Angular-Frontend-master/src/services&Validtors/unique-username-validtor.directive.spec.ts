import { UniqueUsernameValidtorDirective } from './unique-username-validtor.directive';

describe('UniqueUsernameValidtorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueUsernameValidtorDirective();
    expect(directive).toBeTruthy();
  });
});
