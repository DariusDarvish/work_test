
import {AbstractControl} from '@angular/forms'
import { HttpClient } from '@angular/common/http';




export function companyValidator(control: AbstractControl){

    if(control && control.value !== null || control.value !==undefined ||){
        
    }

}